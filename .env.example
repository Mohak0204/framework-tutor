# Local PostgreSQL credentials used by docker-compose.yml.
POSTGRES_USER=framework_tutor
POSTGRES_PASSWORD=replace-with-a-local-password
# Base64-encoded 32-byte minimum secret for application JWT signing. Generate your own for local use.
JWT_SECRET=replace-with-a-base64-encoded-secret
